import styled from 'styled-components';

import theme from '@/theme';

export const SpinnerContainer = styled.div`
  display: flex;
  justify-content: center;
  align-items: center;
  height: ${theme.spaces[14]}vh;
`;
