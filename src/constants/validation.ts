export const enum ERROR_MESSAGE {
  REQUIRED = 'Field is required',
  MAIL = 'Invalid email format',
  MAX_SYMBOLS = 'Maximum number of characters',
  MIN_SYMBOLS = 'Minimum number of characters',
}

export const enum AMOUNT_SYMBOLS {
  MAX_NAME = 50,
  MIN_THEME = 5,
  MAX_THEME = 50,
  MAX_MESSAGE = 300,
}
